#!/usr/bin/bash
set -euo pipefail

DIR=${1:-.}

cd $DIR

URL=$(/usr/bin/git remote get-url origin)

if [[ "$URL" == "https://"* ]]; then
    sensible-browser ${URL//.git/}
else
    PARTS=($(echo ${URL} | sed 's/[@:\.]/ /g'))

    sensible-browser "https://github.com/${PARTS[4]}"
fi
